# Environment variables (contains secrets)
# Covers: .env, .env.local, .env.production, .env.development, etc.
.env
.env.*
!.env.example
.env.*.local

# Backup files (may contain secrets)
*.bak
*.backup
*.orig

# AI Agent Instructions
CLAUDE.md
AGENTS.md
GEMINI.md

# Data directories (persistent container data, keep directory structure)
# Ignore all files but preserve directories and .gitkeep
data/**
!data/
!data/**/
!data/**/.gitkeep

# Backup files (keep directory structure)
backups/**/*
!backups/**/.gitkeep

# Log files (keep directory structure)
logs/**/*
!logs/**/.gitkeep

# Database dumps (outside of backups/)
*.sql.gz
*.dump
*.dump.gz

# Private keys and certificates
*.pem
*.key
*.p12
*.pfx
id_rsa*
*.crt

# Docker
.docker/config.json

# IDE and editor files
.idea/
.vscode/
*.swp
*.swo
*~

# OS generated files
.DS_Store
Thumbs.db

# Runtime files
*.pid
*.seed
*.pid.lock
# Spec System (AI development workflow)
.spec_system/
